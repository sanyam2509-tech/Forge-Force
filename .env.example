# Optional: enables real AI generation via Google Gemini. Falls back to mock data if not set.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
RESEND_API_KEY=         # For real email sending
RESEND_FROM_EMAIL=      # Sender address (e.g. events@yourdomain.com). Defaults to onboarding@resend.dev
